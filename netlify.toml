[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — sve nepoznate rute idu na index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
